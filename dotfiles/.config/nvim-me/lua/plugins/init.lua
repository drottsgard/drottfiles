-- Add plugin specs as separate files in this directory.
-- Each file should return a table (or list of tables) with lazy.nvim plugin specs.
-- Example: lua/plugins/colorscheme.lua
--
--   return {
--     "catppuccin/nvim",
--     name = "catppuccin",
--     lazy = false,
--     priority = 1000,
--     config = function()
--       vim.cmd.colorscheme("catppuccin")
--     end,
--   }
--
-- lazy.nvim automatically imports all files in this directory.

return {}
